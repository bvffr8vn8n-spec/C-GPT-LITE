# 📋 Что осталось выполнить в разделе 4

## ⚠️ ТРЕБОВАНИЕ 4.5: Меншоны диапазонов (частично выполнено)

### Что уже есть:
✅ Формирование меншона в формате `@Sheet1!A1:B3`  
✅ Кнопка "Вставить {selectedRange}" в модалке `TableModal`  
✅ Callback `onRangeSelect` в `TableModal`  

### Что нужно добавить:

#### 1. Интеграция `onRangeSelect` с полем ввода в `ChatClient.tsx`

**Проблема:** `TableMessage` принимает `onRangeSelect` prop, но он не передаётся и не используется в `ChatClient.tsx`.

**Что нужно сделать:**

1. **В `ChatClient.tsx` добавить обработчик для вставки меншона:**
   ```typescript
   const handleRangeSelect = (range: string) => {
     // Вставить меншон в поле ввода
     setText((prev) => {
       const newText = prev ? `${prev} ${range}` : range;
       return newText;
     });
     // Опционально: фокус на поле ввода
     // inputRef.current?.focus();
   };
   ```

2. **Передать `onRangeSelect` в `TableMessage` (если он используется):**
   ```typescript
   <TableMessage 
     tableData={...} 
     onRangeSelect={handleRangeSelect}
   />
   ```

3. **Или добавить обработчик в `SpreadsheetView` (если таблица открывается через него):**
   - Сейчас `SpreadsheetView` используется для отображения таблицы из `getRange`
   - Нужно добавить возможность открыть модалку с выделением ячеек
   - Или добавить `onRangeSelect` callback в `SpreadsheetView`

**Файлы для изменения:**
- `app/threads/[id]/ChatClient.tsx` - добавить `handleRangeSelect` и передать в компоненты
- `app/threads/[id]/SpreadsheetView.tsx` - добавить возможность открыть модалку или callback

---

## ⚠️ ТРЕБОВАНИЕ 4.6: Интеграция с агентом (частично выполнено)

### Что уже есть:
✅ Модель может работать с таблицей через tools  
✅ System prompt содержит инструкции для работы с XLSX  
✅ Все tools подключены и доступны модели  

### Что нужно добавить:

#### 1. Явная обработка меншонов в system prompt

**Проблема:** System prompt не содержит инструкций по парсингу меншонов формата `@Sheet1!A1:B3`.

**Что нужно сделать:**

Добавить в system prompt (в `app/api/chat/route.ts`, строка ~165) инструкции:

```typescript
const systemPrompt = `Ты полезный ассистент.

// ... существующие инструкции ...

Для работы с меншонами диапазонов (формат @Sheet1!A1:B3):
1. Если пользователь упоминает диапазон в формате @Sheet1!A1:B3 или @Sheet1!A1 - это ссылка на ячейки в таблице
2. Парси формат: @<SheetName>!<CellOrRange>
   - Пример: @Sheet1!A1 - одна ячейка A1 на листе Sheet1
   - Пример: @Sheet1!A1:B3 - диапазон от A1 до B3 на листе Sheet1
3. Используй эти параметры для вызова getRange:
   - sheet: имя листа из меншона (например "Sheet1")
   - from: начальная ячейка (например "A1")
   - to: конечная ячейка (если указан диапазон) или та же ячейка (если указана одна)
4. Примеры использования:
   - "Объясни формулу в @Sheet1!D4" → explainFormula({ sheet: "Sheet1", cell: "D4" })
   - "Покажи данные из @Sheet1!A1:B3" → getRange({ sheet: "Sheet1", from: "A1", to: "B3" })
   - "Возьми email из @Sheet1!B2:B5" → getRange({ sheet: "Sheet1", from: "B2", to: "B5" }), затем обработай данные
`;
```

**Файлы для изменения:**
- `app/api/chat/route.ts` (строки 165-177) - добавить инструкции по меншонам

---

## 📝 Итоговый список задач

### Задача 1: Интеграция меншонов с полем ввода
**Приоритет:** Высокий  
**Файлы:**
- `app/threads/[id]/ChatClient.tsx`
- `app/threads/[id]/SpreadsheetView.tsx` (опционально)

**Шаги:**
1. Добавить функцию `handleRangeSelect(range: string)` в `ChatClient.tsx`
2. Функция должна вставлять меншон в поле ввода (`setText`)
3. Передать `onRangeSelect={handleRangeSelect}` в компоненты, которые могут открыть модалку
4. Проверить, что `TableMessage` или `SpreadsheetView` могут открыть модалку с выделением

### Задача 2: Добавить инструкции по меншонам в system prompt
**Приоритет:** Средний  
**Файлы:**
- `app/api/chat/route.ts`

**Шаги:**
1. Добавить секцию в system prompt про обработку меншонов
2. Описать формат `@Sheet1!A1:B3`
3. Объяснить, как парсить и использовать меншоны в tool calls
4. Привести примеры использования

---

## ✅ Проверка после выполнения

После выполнения обеих задач нужно проверить:

1. **Меншоны:**
   - [ ] Выделить ячейки в модалке
   - [ ] Нажать "Вставить @Sheet1!A1:B3"
   - [ ] Меншон появился в поле ввода
   - [ ] Отправить сообщение с меншоном
   - [ ] Модель правильно поняла меншон и вызвала tool

2. **Интеграция с агентом:**
   - [ ] Отправить: "Объясни формулу в @Sheet1!D4"
   - [ ] Модель вызвала `explainFormula({ sheet: "Sheet1", cell: "D4" })`
   - [ ] Отправить: "Покажи данные из @Sheet1!A1:B3"
   - [ ] Модель вызвала `getRange({ sheet: "Sheet1", from: "A1", to: "B3" })`

---

## 📊 Статус выполнения

**Текущий статус:** 8/10 требований полностью выполнено (80%)  
**После выполнения задач:** 10/10 требований полностью выполнено (100%)

