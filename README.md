# ChatGPT-lite

Упрощённый аналог интерфейса ChatGPT с тредами, хранением сообщений в базе и **генеративным UI** на основе Vercel AI SDK.

---

## 🚀 Пошаговая инструкция по запуску проекта

### Шаг 1: Проверка установки Bun

Откройте терминал (PowerShell на Windows, Terminal на macOS/Linux) и выполните:

```bash
bun --version
```

**Если команда не найдена** — перейдите к [Шагу 2: Установка Bun](#шаг-2-установка-bun)

**Если версия 1.3 или выше** — переходите к [Шагу 3: Подготовка проекта](#шаг-3-подготовка-проекта)

---

### Шаг 2: Установка Bun

#### Windows (PowerShell):
```powershell
powershell -c "irm bun.sh/install.ps1 | iex"
```

После установки:
- Закройте и откройте терминал заново
- Или выполните: `refreshenv` (если установлен через Chocolatey)

#### macOS/Linux:
```bash
curl -fsSL https://bun.sh/install | bash
```

После установки:
- Закройте и откройте терминал заново
- Или выполните: `source ~/.bashrc` (или `source ~/.zshrc`)

#### Проверка установки:
```bash
bun --version
```
Должна быть версия **1.3+**

---

### Шаг 3: Подготовка проекта

1. **Перейдите в папку проекта**:
   ```bash
   cd chatgpt-lite
   ```

2. **Проверьте структуру проекта**:
   ```bash
   # Windows
   dir
   
   # macOS/Linux
   ls -la
   ```

   Должны быть видны файлы:
   - `package.json`
   - `app/`
   - `lib/`
   - `data/`

---

### Шаг 4: Установка зависимостей

Выполните команду:

```bash
bun install
```

**Что происходит:**
- Устанавливаются все зависимости из `package.json`
- Создаётся папка `node_modules/`
- Создаётся файл `bun.lockb`

**Ожидаемое время:** 1-3 минуты

**Если возникли ошибки:**
- Убедитесь, что Bun установлен: `bun --version`
- Удалите `node_modules` и `bun.lockb` (если есть) и повторите установку

---

### Шаг 5: Настройка API ключа OpenAI

1. **Получите API ключ OpenAI**:
   - Зайдите на https://platform.openai.com/api-keys
   - Войдите в аккаунт (или создайте новый)
   - Нажмите "Create new secret key"
   - Скопируйте ключ (он начинается с `sk-proj-` или `sk-`)

2. **Создайте файл `.env.local`** в корне проекта:

   **Вариант 1 (через терминал):**
   ```bash
   # Windows (PowerShell)
   echo "OPENAI_API_KEY=ваш_ключ_здесь" > .env.local
   
   # macOS/Linux
   echo "OPENAI_API_KEY=ваш_ключ_здесь" > .env.local
   ```

   **Вариант 2 (вручную):**
   - Создайте файл `.env.local` в корне проекта (там же, где `package.json`)
   - Откройте файл в текстовом редакторе
   - Добавьте строку:
     ```env
     OPENAI_API_KEY=sk-proj-ваш_ключ_здесь
     ```
   - Сохраните файл

3. **Проверьте файл**:
   ```bash
   # Windows
   type .env.local
   
   # macOS/Linux
   cat .env.local
   ```

   Должна быть строка: `OPENAI_API_KEY=sk-proj-...`

   **⚠️ Важно:** 
   - Не добавляйте пробелы вокруг `=`
   - Не используйте кавычки вокруг ключа
   - Файл `.env.local` уже добавлен в `.gitignore` и не попадёт в Git

---

### Шаг 6: Инициализация базы данных

База данных создаётся **автоматически** при первом запуске, но можно проверить:

1. **Проверьте папку `data/`**:
   ```bash
   # Windows
   dir data
   
   # macOS/Linux
   ls -la data
   ```

2. **Если папки `data/` нет** — она создастся автоматически при запуске

3. **Проверьте наличие XLSX файла**:
   ```bash
   # Windows
   dir data\example.xlsx
   
   # macOS/Linux
   ls -la data/example.xlsx
   ```

   Если файла нет, создайте его:
   ```bash
   bun run scripts/create-example-xlsx.ts
   ```

---

### Шаг 7: Запуск проекта

#### Вариант 1: С автозапуском браузера (рекомендуется)

```bash
bun run dev
```

**Что происходит:**
- Запускается Next.js dev сервер
- Через 3 секунды автоматически откроется браузер на http://localhost:3000
- В терминале будут видны логи сервера

#### Вариант 2: Без автозапуска браузера

```bash
bun run dev:simple
```

**Что происходит:**
- Запускается Next.js dev сервер
- Браузер не открывается автоматически
- Откройте вручную: http://localhost:3000

---

### Шаг 8: Проверка работы

1. **Откройте браузер**: http://localhost:3000

2. **Должна открыться главная страница** с приветствием

3. **Перейдите к чатам**:
   - Нажмите кнопку "Открыть чаты" или
   - Перейдите на http://localhost:3000/threads

4. **Создайте первый тред**:
   - Введите название в поле "Название треда" (например, "Тестовый чат")
   - Нажмите "Создать"
   - ✅ Тред должен появиться в сайдбаре слева

5. **Отправьте тестовое сообщение**:
   - Введите: "Привет!"
   - Нажмите Enter или кнопку отправки
   - ✅ Ответ должен стримиться в реальном времени
   - ✅ Сообщение должно сохраниться в БД

6. **Проверьте базу данных** (опционально):
   - Откройте http://localhost:3000/api/db-check
   - Должны быть видны таблицы `threads` и `messages`

---

### Шаг 9: Остановка сервера

В терминале, где запущен сервер, нажмите:

```
Ctrl + C
```

Сервер остановится, и вы вернётесь в командную строку.

---

## 🚀 Быстрый старт (краткая версия)

Если вы уже знакомы с проектом:

```bash
# 1. Установить зависимости
bun install

# 2. Создать .env.local с API ключом
echo "OPENAI_API_KEY=your_key_here" > .env.local

# 3. Запустить проект
bun run dev
```

Откройте http://localhost:3000 в браузере.

---

## 📦 Установка Bun (если не установлен)

### Windows:
```powershell
powershell -c "irm bun.sh/install.ps1 | iex"
```

### macOS/Linux:
```bash
curl -fsSL https://bun.sh/install | bash
```

### Проверка установки:
```bash
bun --version
```
Должна быть версия **1.3+**

---

## 🛠 Команды для работы

### Разработка:
```bash
bun run dev              # Запуск с автозапуском браузера
bun run dev:simple       # Запуск без автозапуска браузера
```

### Production:
```bash
bun run build            # Сборка проекта
bun run start            # Запуск production версии
```

### Утилиты:
```bash
bun run lint             # Проверка кода
```

---

## ⚙️ Конфигурация

### Переменные окружения

Создайте файл `.env.local` в корне проекта:

```env
OPENAI_API_KEY=your_openai_api_key_here
```

**Важно:** 
- Файл `.env.local` не должен попадать в Git (уже добавлен в `.gitignore`)
- После изменения `.env.local` перезапустите dev сервер

### Инициализация базы данных

База данных SQLite создаётся **автоматически** при первом запуске в папке `data/chat.db`.

### XLSX файл

Пример таблицы находится в `data/example.xlsx`. Файл создаётся автоматически при первом запуске, или можно создать вручную:

```bash
bun run scripts/create-example-xlsx.ts
```

---

## 📋 Реализованные функции

### ✅ 1. Чат с тредами

- ✅ Список тредов в сайдбаре слева
- ✅ Создание нового треда
- ✅ Переключение между тредами
- ✅ Загрузка истории сообщений из БД при переключении
- ✅ Streaming ответы от модели (SSE)
- ✅ Сохранение всех сообщений (user и assistant) в SQLite
- ✅ Персистентность: все сообщения сохраняются в базе данных

### ✅ 2. Клиентские tools (Generative UI)

- ✅ `askForConfirmation` - запрос подтверждения у пользователя
- ✅ `getLocation` - получение локации (автоматически выполняется)
- ✅ `requestDangerousActionConfirmation` - подтверждение опасных действий
- ✅ `openTable` - открытие таблицы сообщений в UI

### ✅ 3. Подтверждение опасных действий

- ✅ Двухэтапный процесс: запрос подтверждения → выполнение действия
- ✅ UI компоненты для подтверждения (карточки с кнопками Да/Нет)
- ✅ Поддержка действий:
  - `deleteThread` - удаление треда
  - `deleteMessage` - удаление сообщения (через модель)
  - `updateMessage` - обновление сообщения (через модель)
  - `updateXlsxCell` - обновление ячейки XLSX

### ✅ 4. Таблица сообщений треда

- ✅ `getThreadMessagesTable` - server tool для получения сообщений треда в виде таблицы
- ✅ `openTable` - client tool для открытия UI таблицы
- ✅ Визуальное отображение таблицы сообщений с колонками: ID, Роль, Содержание, Дата
- ✅ Редактирование сообщений через inline edit в таблице
- ✅ Удаление сообщений через кнопку в таблице
- ✅ Подтверждение опасных действий (редактирование/удаление) через 2-этапный процесс
- ✅ Автоматическое обновление таблицы после выполнения операций

### ✅ 5. Работа с XLSX таблицей

- ✅ Tools для работы с таблицей:
  - `getRange` - чтение диапазона ячеек
  - `updateCell` - запись в ячейку (с подтверждением)
  - `explainFormula` - объяснение формул
  - `getSheets` - список листов
- ✅ Визуальное отображение таблицы в чате (мини-превью)
- ✅ Модальное окно с полной таблицей
- ✅ Выделение ячеек мышкой в модалке
- ✅ Меншоны диапазонов (например, `@Sheet1!A1:B3`)
- ✅ Вставка меншонов в поле ввода чата
- ✅ Интеграция с агентом: понимание меншонов и использование соответствующих tools
- ✅ Поддержка формул Excel (SUM, AVERAGE, MIN, MAX, арифметические операции)
- ✅ Автоматический пересчёт формул при изменении ячеек

---

## 📝 Примеры команд для тестирования функционала

### 1. Чат с тредами

**Создание и работа с тредами:**
```
"Привет! Что умеешь?"
"Расскажи о себе"
"Как работает этот чат?"
```

**Проверка персистентности:**
1. Создайте тред и отправьте несколько сообщений
2. Переключитесь на другой тред
3. Вернитесь к первому треду
4. ✅ История сообщений должна загрузиться из БД

---

### 2. Клиентские tools (Generative UI)

**askForConfirmation - запрос подтверждения:**
```
"Спроси у меня подтверждение на удаление файла"
"Нужно подтверждение для действия X"
"Запроси подтверждение: готовы ли вы продолжить?"
```

**getLocation - получение локации:**
```
"Где я нахожусь?"
"Какой мой город?"
"Определи моё местоположение"
```

**openTable - открытие таблицы:**
```
"Покажи таблицу сообщений"
"Открой таблицу сообщений треда"
"Покажи сообщения в виде таблицы"
```

---

### 3. Подтверждение опасных действий

**Удаление треда:**
```
"Удали этот тред"
"Удалить текущий тред"
"Удалить чат"
```
✅ Должна появиться карточка подтверждения с кнопками "Да"/"Нет"

**Удаление сообщения:**
```
"Удали сообщение [id]"
"Удалить последнее сообщение"
"Удали моё предыдущее сообщение"
```
✅ Должна появиться карточка подтверждения

**Редактирование сообщения:**
```
"Измени сообщение [id] на: новый текст"
"Отредактируй последнее сообщение на: исправленный текст"
```
✅ Должна появиться карточка подтверждения

**Обновление ячейки XLSX:**
```
"Измени ячейку Sheet1!C2 на 5000"
"Обнови ячейку Sheet1!B3 на значение 'Новое'"
"Поменяй Sheet1!A1 на 100"
```
✅ Должна появиться карточка подтверждения

---

### 4. Таблица сообщений треда

**Показ таблицы сообщений:**
```
"Покажи таблицу сообщений"
"Покажи сообщения в виде таблицы"
"Открой таблицу сообщений треда"
"Выведи все сообщения в таблице"
```
✅ Должен вызваться tool `getThreadMessagesTable`, затем `openTable`
✅ В чате появится таблица с колонками: ID, Роль, Содержание, Дата

**Редактирование через таблицу:**
1. Откройте таблицу сообщений (см. команды выше)
2. Нажмите кнопку "✏️" напротив сообщения
3. Измените текст в inline редакторе
4. Нажмите "✓" (Save)
5. ✅ Должна появиться карточка подтверждения
6. Нажмите "Да"
7. ✅ Сообщение обновится в таблице и в чате

**Удаление через таблицу:**
1. Откройте таблицу сообщений
2. Нажмите кнопку "🗑️" напротив сообщения
3. ✅ Должна появиться карточка подтверждения
4. Нажмите "Да"
5. ✅ Сообщение удалится из таблицы и из чата

---

### 5. Работа с XLSX таблицей

**Чтение диапазона ячеек:**
```
"Покажи таблицу из Sheet1"
"Прочитай диапазон Sheet1!A1:D6"
"Покажи данные из Sheet1, ячейки A1 до C5"
"Выведи Sheet1!B2:B10"
```
✅ Должен вызваться tool `getRange`
✅ В чате появится мини-превью таблицы
✅ При клике на таблицу откроется модалка с полной таблицей

**Обновление ячейки:**
```
"Измени ячейку Sheet1!C2 на 5000"
"Обнови Sheet1!B3 на значение 'Новое значение'"
"Поменяй Sheet1!A1 на 100"
"Запиши в Sheet1!D5 число 42"
```
✅ Должен вызваться tool `updateCell`
✅ Должна появиться карточка подтверждения
✅ После подтверждения ячейка обновится

**Объяснение формулы:**
```
"Объясни формулу в ячейке Sheet1!D6"
"Что делает формула в Sheet1!C5?"
"Расскажи про формулу Sheet1!B10"
```
✅ Должен вызваться tool `explainFormula`
✅ Появится карточка с объяснением формулы, зависимостями и вычисленным значением

**Список листов:**
```
"Какие листы есть в таблице?"
"Покажи список листов"
"Какие листы доступны?"
```
✅ Должен вызваться tool `getSheets`
✅ Вернётся список всех листов в XLSX файле

**Работа с формулами:**
```
"Покажи ячейку Sheet1!D6" (если там формула, покажет результат)
"Вычисли сумму в Sheet1!D2:D5"
"Покажи среднее значение из Sheet1!C2:C10"
```
✅ Формулы автоматически вычисляются при чтении диапазона

**Меншоны диапазонов:**
1. Откройте таблицу (кликните на мини-превью)
2. Выделите диапазон ячеек мышкой (например, A1:B3)
3. Нажмите кнопку "Вставить меншон"
4. ✅ В поле ввода появится: `@Sheet1!A1:B3`
5. Используйте в запросах:
   ```
   "Объясни формулу в @Sheet1!D6"
   "Покажи данные из @Sheet1!A1:B3"
   "Сумма из @Sheet1!C2:C5"
   ```
✅ Агент должен понять меншон и использовать соответствующий tool

**Работа с формулами через меншоны:**
```
"Объясни формулу в @Sheet1!D6"
"Что делает формула @Sheet1!C5?"
"Вычисли @Sheet1!D2:D5"
```
✅ Агент понимает меншоны и вызывает `explainFormula` или `getRange`

---

## ⚠️ Частично реализованные функции

### 🔶 Удаление и редактирование сообщений через контекстное меню

**Статус:** Реализовано во фронтенде, но не полностью интегрировано с backend.

**Что работает:**
- ✅ Контекстное меню на сообщениях пользователя (правый клик)
- ✅ Кнопки "Изменить" и "Удалить" в меню
- ✅ Модальное окно для редактирования сообщения
- ✅ Отправка команды модели для удаления/редактирования

**Что не реализовано:**
- ❌ Прямое удаление/редактирование через API без участия модели
- ❌ Мгновенное обновление UI после удаления/редактирования (требуется ответ модели)

**Как это работает сейчас:**
1. Пользователь нажимает "Удалить" или "Изменить" в контекстном меню
2. Отправляется текстовая команда модели (например, `"Удалить сообщение [id]"`)
3. Модель вызывает `requestDangerousActionConfirmation` → `performDangerousAction`
4. Действие выполняется через backend API

**В отличие от остального функционала:**
- Удаление/редактирование через таблицу сообщений работает полностью (с подтверждением и мгновенным обновлением)
- Удаление треда работает полностью (с подтверждением и редиректом)
- Обновление XLSX ячеек работает полностью (с подтверждением и обновлением таблицы)

---

## 🛠 Технологический стек

- **Next.js 16** (App Router, TypeScript)
- **Vercel AI SDK UI**:
  - `useChat` hook для чата
  - Generative UI / tools для интерактивных компонентов
- **Bun 1.3+** с встроенной SQLite (`bun:sqlite`) для хранения тредов и сообщений
- **XLSX** (`xlsx`) для работы с таблицами
- **formulajs** для вычисления Excel формул
- **Zod** для валидации схем tools
- **OpenAI API** (gpt-4o-mini)
- **Tailwind CSS** для стилизации

---

## 📁 Структура проекта

Проект организован по принципу разделения слоёв (API / UI / DB):

```
chatgpt-lite/
├── app/                          # Next.js App Router
│   ├── api/                      # API слой (Backend)
│   │   ├── chat/route.ts         # POST /api/chat - streaming чат
│   │   ├── threads/              # API для тредов
│   │   ├── messages/             # API для сообщений
│   │   └── xlsx/                 # API для работы с XLSX
│   ├── threads/                  # UI слой (Frontend)
│   │   ├── [id]/
│   │   │   ├── page.tsx          # Server Component страницы треда
│   │   │   ├── ChatClient.tsx     # Client Component чата (useChat, Generative UI)
│   │   │   ├── TableMessage.tsx  # Компонент отображения XLSX таблицы
│   │   │   ├── MessagesTable.tsx  # Компонент таблицы сообщений треда
│   │   │   ├── EditMessageModal.tsx # Модальное окно редактирования
│   │   │   ├── SpreadsheetView.tsx # Интерактивная таблица XLSX
│   │   │   └── components/       # UI компоненты для tools
│   │   │       ├── ConfirmationCard.tsx
│   │   │       ├── DangerousActionConfirmationCard.tsx
│   │   │       ├── LocationPermissionCard.tsx
│   │   │       ├── FormulaExplanationCard.tsx
│   │   │       └── MessageContextMenu.tsx
│   │   └── page.tsx              # Список тредов
│   └── layout.tsx
├── lib/                          # Бизнес-логика
│   ├── db/                       # DB слой
│   │   ├── client.ts            # Инициализация Bun SQLite
│   │   └── chat-store.ts        # CRUD операции для threads/messages
│   ├── tools/                    # AI SDK Tools
│   │   ├── chat-tools.ts        # Клиентские tools (Generative UI)
│   │   ├── server-tools.ts      # Серверные tools (опасные действия)
│   │   └── xlsx-tools.ts        # Tools для работы с XLSX
│   └── xlsx/
│       ├── table-manager.ts      # Управление XLSX файлом
│       ├── formula-calculator.ts # Вычисление формул Excel
│       └── formula-parser.ts    # Парсинг зависимостей формул
├── data/
│   ├── chat.db                  # SQLite база данных (создаётся автоматически)
│   └── example.xlsx             # Пример таблицы
└── scripts/                     # Утилиты
    ├── create-example-xlsx.ts   # Создание примера XLSX
    └── test-*.ts                # Тестовые скрипты
```

---

## 🗄 База данных

### Таблица `threads`
- `id` (TEXT PRIMARY KEY)
- `title` (TEXT NOT NULL)
- `created_at` (INTEGER NOT NULL)

### Таблица `messages`
- `id` (TEXT PRIMARY KEY)
- `thread_id` (TEXT NOT NULL, FOREIGN KEY)
- `role` (TEXT NOT NULL, CHECK: 'user' | 'assistant')
- `content` (TEXT NOT NULL) - JSON строка с parts для assistant сообщений
- `created_at` (INTEGER NOT NULL)

База данных создаётся автоматически при первом запуске в `data/chat.db`.

---

## 💡 Краткая справка по командам

Для быстрого доступа к примерам команд см. раздел [📝 Примеры команд для тестирования функционала](#-примеры-команд-для-тестирования-функционала) выше.

**Самые популярные команды:**
- `"Привет! Что умеешь?"` - обычный запрос
- `"Покажи таблицу сообщений"` - откроет таблицу с сообщениями
- `"Покажи таблицу из Sheet1"` - покажет XLSX таблицу
- `"Измени ячейку Sheet1!C2 на 5000"` - обновит ячейку (с подтверждением)
- `"Удали этот тред"` - удалит тред (с подтверждением)

---

## 🔧 API Endpoints

### `POST /api/chat`
Отправка сообщения в чат (streaming)

**Body:**
```json
{
  "id": "thread-id",
  "messages": [...]
}
```

### `GET /api/threads`
Получить список всех тредов

### `POST /api/threads`
Создать новый тред

**Body:**
```json
{
  "title": "Название треда"
}
```

### `DELETE /api/threads/[id]`
Удалить тред

### `GET /api/threads/[id]/messages`
Получить все сообщения треда

### `GET /api/xlsx/range`
Получить диапазон ячеек из XLSX

**Query params:**
- `sheet` - название листа
- `from` - начальная ячейка (например, A1)
- `to` - конечная ячейка (например, D6)

### `POST /api/xlsx/perform`
Выполнить действие с XLSX (обновление ячейки)

**Body:**
```json
{
  "action": "updateXlsxCell",
  "targetId": "C2",
  "newContent": "Sheet1",
  "xlsxValue": "5000"
}
```

---

## ⚠️ Ограничения и известные проблемы

### Ограничения:
- ❌ E2E тесты на Playwright (не реализовано)
- ⚠️ XLSX файл захардкожен (`data/example.xlsx`)
- ⚠️ Поддержка только одного XLSX файла
- ⚠️ Нет поддержки создания новых листов в XLSX
- ⚠️ Удаление/редактирование сообщений через контекстное меню работает через модель (см. раздел "Частично реализованные функции")

### Известные проблемы:
- При удалении/редактировании сообщения через контекстное меню требуется ответ модели для обновления UI
- Некоторые сложные Excel формулы могут не поддерживаться (поддерживаются: SUM, AVERAGE, MIN, MAX, COUNT, арифметические операции)

---

## 📊 Что реализовано полностью

✅ Чат с тредами и персистентностью  
✅ Streaming ответы от модели (SSE UIMessage stream)  
✅ Клиентские tools (Generative UI)  
✅ Подтверждение опасных действий (двухэтапный процесс)  
✅ Таблица сообщений треда с редактированием/удалением  
✅ Работа с XLSX (чтение, запись, формулы)  
✅ Визуальное отображение таблиц  
✅ Модальное окно с выделением ячеек  
✅ Меншоны диапазонов  
✅ Автоматический пересчёт формул Excel  
✅ Обработка ошибок  
✅ Логирование этапов обработки запросов  

---

## 🧪 Тестирование

### Базовые тесты:

1. **Создание треда:**
   - Откройте http://localhost:3000/threads
   - Введите название и нажмите "Создать"
   - ✅ Тред должен появиться в сайдбаре

2. **Отправка сообщения:**
   - Выберите тред
   - Отправьте: "Привет!"
   - ✅ Ответ должен стримиться в реальном времени

3. **Переключение между тредами:**
   - Создайте несколько тредов
   - Переключайтесь между ними
   - ✅ История сообщений должна загружаться из БД

4. **Работа с XLSX:**
   - Отправьте: "Покажи таблицу из Sheet1"
   - ✅ Должно появиться мини-превью таблицы
   - Кликните на таблицу
   - ✅ Должна открыться модалка

5. **Подтверждение действий:**
   - Отправьте: "Удали этот тред"
   - ✅ Должна появиться карточка подтверждения
   - Нажмите "Да"
   - ✅ Тред должен удалиться

---

## 📚 Полезные ссылки

- [Vercel AI SDK Documentation](https://ai-sdk.dev/)
- [Next.js Documentation](https://nextjs.org/docs)
- [Bun Documentation](https://bun.sh/docs)
- [OpenAI API Documentation](https://platform.openai.com/docs)

---

## 📄 Лицензия

MIT
